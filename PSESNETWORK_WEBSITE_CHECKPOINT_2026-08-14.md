# نقطة استئناف إعادة بناء psesnetwork.com

التاريخ: 2026-08-14  
المستودع: `obaeda/psesnetwork`  
المسار المحلي: `/Users/obaydahshareef/Desktop/the most important file /psesnetwork`

## القرار

مشروع PSES السابق للبلوك تشين والحوسبة الموزعة أُلغي. أُزيل محتواه وصوره من النسخة العامة الجديدة، مع بقاء تاريخ Git كمسار رجوع فقط. الهوية الحالية الوحيدة للموقع هي **PSES — السوق الفلسطيني الذكي**.

لم يُحذف مستودع GitHub ولا تاريخ الموقع. نُشر الموقع الجديد حيًا على `psesnetwork.com` من فرع `main`، وDNS الحالي:

- A records إلى GitHub Pages: `185.199.108.153` حتى `185.199.111.153`.
- `www` يشير إلى `obaeda.github.io` ويحوّل إلى الدومين الرئيسي.
- `app.psesnetwork.com` يشير إلى Firebase Hosting ويبقى لروابط المنتجات، الإحالة، App Links والصفحات التشغيلية.

## ما تم بناؤه

1. واجهة جديدة داكنة بنفسجية/ذهبية متوافقة مع هوية التطبيق ومتجاوبة مع الهاتف وسطح المكتب.
2. صفحة رئيسية عربية وإنجليزية.
3. فهرس مزايا كامل عربي وإنجليزي، مع وصف وحدود وحالة كل ميزة.
4. FAQ عربي وإنجليزي مع JSON-LD مطابق للنص الظاهر.
5. مقال عربي وإنجليزي عن تطبيقات التسوق في فلسطين، الثقة، مطابقة المنتج، الذكاء المفيد وروابط إعلانات المنتجات.
6. صفحات عن PSES، التحميل وحالة الإصدار، الخصوصية وحذف الحساب باللغتين.
7. `robots.txt`, `sitemap.xml`, `feed.xml`, `site.webmanifest` و404 مخصصة.
8. `llms.txt`, `llms-full.txt` و`knowledge.json` لتقديم حقائق قابلة للقراءة الآلية، مع منع ادعاءات البلوك تشين/العملة/الترتيب/الإطلاق المزيفة.
9. تحويل صفحات الروابط القديمة `ai.html`, `market.html`, `wallet.html`, `network.html`, `docs.html`, `status/` إلى المسارات الحالية مع `noindex`.
10. صفحات المنتجات الديناميكية بقيت على `app.psesnetwork.com/p/**` لأنها مخصصة لبطاقات المشاركة والإعلانات وفتح التطبيق/المتجر. لا يوجد كتالوج Static يتقادم.

## روابط المنتجات وبطاقات Meta

- حُدّثت ونُشرت دالة `openProductLinkV2` وحدها على مشروع العملاء `pses-palestine` بتاريخ 2026-08-14.
- زاحف Facebook يحصل على اسم المنتج، صورته العامة المعتمدة، السعر الظاهر، `ILS`، وبيانات Open Graph وProduct JSON-LD.
- المتصفح العادي يحصل على تحويل `302` إلى Google Play مع `product_id` وكود الإحالة الآمن داخل Install Referrer.
- إذا كان التطبيق مثبتًا، Android App Link للمسار `/p/{productId}` يفتح صفحة المنتج قبل الوصول للويب.
- الـcanonical و`og:url` لا يحتويان كود الإحالة؛ الإحالة محفوظة لمسار التثبيت فقط.
- فحص حي على المنتج `clothes__women-satin-blouse-260138` أثبت صورة عامة ترجع `200` وسعر `90.00 ILS` وتحويل Play يحفظ المعرّف.
- النسخة المنشورة: `openproductlinkv2-00002-tux`، ونتيجة النشر Function واحدة ناجحة و0 أخطاء.
- لا فهرس منتجات ثابت بالموقع؛ مصدر البطاقة دائمًا مستند المنتج النشط الموثوق من Firestore.

## الغلاف البصري

استُخدمت أداة imagegen المدمجة لإنشاء غلاف أصلي بلا نص أو شعارات أو تقييمات وهمية. الملفات النهائية:

- `assets/pses-market-hero.jpg`
- `assets/pses-market-hero-1280.jpg`
- `assets/pses-market-hero-768.jpg`
- `assets/pses-market-hero-1200x630.jpg` للمشاركة وOpen Graph.

ملخص prompt: تصوير تجاري واقعي عريض لسوق فلسطيني ذكي يجمع ملابس وبقالة وطرد توصيل وهاتف بواجهة داكنة مجردة، العناصر في يمين الصورة ومساحة هادئة للنص، ألوان فحمية وبنفسجية ولمسة ذهبية، بلا أشخاص أو نص أو علامات تجارية أو شعارات.

## التحقق الفعلي

- `python3 scripts/validate_site.py`: ناجح؛ 25 صفحة HTML وروابط/JSON-LD/JSON/XML ومحتوى legacy.
- Functions: `npm run lint` ناجح؛ `npm test` أعطى 332 اختبارًا، 282 ناجحًا، 50 Emulator متخطّاة، و0 فشل.
- اختبارات رابط المنتج المركزة: 6/6 ناجحة، وتشمل XSS وcanonical والإحالة والسعر وJSON-LD وتمييز الزاحف عن المتصفح.
- فحص viewport عبر Chrome DevTools:
  - هاتف `390×844`: `innerWidth=390`, `scrollWidth=390`, بدون عناصر خارجة.
  - سطح مكتب `1440×1000`: الشكل متزن والـHero واضح.
- Lighthouse المحلي بعد responsive images:
  - Performance: `100`
  - Accessibility: `100`
  - Best Practices: `100`
  - SEO: `100`
  - FCP: `1.1s`, LCP: `1.7s`, TBT: `0ms`, CLS: `0`.
- استُخدم Firebase Preview منفصل قبل النشر الحي:
  - URL: `https://pses-palestine--psesnetwork-preview-8j4rfmq5.web.app`
  - Hosting version: `9e2f2f7858724127`
  - ينتهي: `2026-08-21 04:27:55` بتوقيت الخليل.
  - المسارات العربية والإنجليزية والمقال والخصوصية والحذف وملفات الفهرسة رجعت HTTP 200.
  - `/products/` يرجع 404 عمدًا لأن المنتجات تُعرض عبر الرابط الديناميكي فقط.

بعد نجاح GitHub Pages حُذفت قناة Preview. أكد `channel:list` بقاء قناة `live` فقط، وأزال Firebase دومين Preview تلقائيًا من Authorized Domains.

## النشر الحي

- Commit الموقع الجديد: `a0303ac1a9c594f3b05953d10af680c5f219fddd`.
- GitHub Pages workflow: `31760743561`، والـbuild والـdeploy ناجحان.
- فُحصت حيًا الصفحة العربية والإنجليزية والمزايا وFAQ والمقال والخصوصية والحذف و`robots.txt` و`sitemap.xml` و`llms.txt` و`knowledge.json`: كلها HTTP 200.
- الصفحة الحية تعرض «السوق الفلسطيني الذكي»، ولا تحتوي كلمات مشروع البلوك تشين الملغي.
- GitHub Pages صار `https_enforced=true` وحالة الموقع `built` مع Custom 404.

## ما لم يُفعّل عن قصد

- لا Meta Pixel ولا Conversions API: ما في Pixel ID، ولا قرار موافقة/قياس نهائي. إضافة Tracking مجهول الآن بتضر الخصوصية ولا تفيد.
- لا Google Analytics للموقع لنفس السبب.
- لا تقييمات نجوم أو أعداد مستخدمين أو «الأفضل» كحقيقة.
- لا موعد إطلاق أو رابط App Store غير موجود.
- لا فهرس منتجات Static؛ صفحة المشاركة الديناميكية هي المرجع حتى تظل الصورة والسعر والحالة حديثة.

## الخطوات المتبقية

1. إرسال `https://psesnetwork.com/sitemap.xml` إلى Google Search Console عند توفر الوصول/الملكية.
2. عند بدء Meta Ads: نحتاج Pixel ID وقرار واضح للموافقة، ثم نربط أحداث مشاهدة المنتج/بدء Checkout/شراء بدون بيانات حساسة.
3. بعد إطلاق V2: تحديث `/download/` بروابط المتاجر الفعلية وحالة الإطلاق، لا قبلها.

## الرجوع إذا فشل النشر

الموقع القديم محفوظ في Git عند commit `3ac8fd0a1b99bc67a6d89dbb4dd43c865eb7004f`. الرجوع المفضل بعد نشر الجديد هو `git revert <new-website-commit>` ثم push، وليس حذف المستودع أو تغيير DNS فورًا.
